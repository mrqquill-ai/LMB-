# The live domain, with protocol and no trailing slash.
# Substituted into index.html at build time for the canonical link, the Open
# Graph card and the structured data. The .example TLD below is a placeholder:
# social previews and search results will not work until it is replaced.
VITE_SITE_URL=https://lagosmusicalband.example
