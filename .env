# The live domain, with protocol and no trailing slash.
# Substituted into index.html at build time for the canonical link, the Open
# Graph card and the structured data. Update this if a custom domain or a
# nicer *.vercel.app alias gets attached later, then redeploy.
VITE_SITE_URL=https://lmb-ruby.vercel.app
